#include<bits/stdc++.h>

vector<int> kthSmallLarge(vector<int> &arr, int n, int k){

    sort (arr.begin(), arr.end());
    int max = arr[k - 1];
    int min = arr[n - k];
    vector<int> ans;
    ans.push_back(max);
    ans.push_back(min);
    return ans;
}
